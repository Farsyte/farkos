// standalone autostager has moved.
// The version that still uses WHEN triggers is in
//    sa/autostager_when.ks
// The code that actually shows it in operation is in
//    sa/autostager_when_demo.ks
runpath("0:sa/autostager_when_demo").

// A version that uses scheduler with a frontend that
// provides pause/resume/cancel is in work.
